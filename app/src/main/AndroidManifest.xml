<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_icon"
        android:roundIcon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.XavierProject"
        tools:targetApi="31">

        <!-- Splash Activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.XavierProject.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Login Type Selection Activity -->
        <activity
            android:name=".LoginTypeActivity"
            android:exported="false"
            android:theme="@style/Theme.XavierProject" />

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- SignUp Activity -->
        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".DiscussionActivity" />
        <activity android:name=".CommentsActivity" />

        <!-- Add these permissions to your AndroidManifest.xml -->
        <!-- Admin Main Activity -->
        <activity
            android:name=".AdminMainActivity"
            android:exported="false"
            android:theme="@style/Theme.XavierProject" />

        <!-- Government Main Activity -->
        <activity
            android:name=".GovMainActivity"
            android:exported="false"
            android:theme="@style/Theme.XavierProject" />
        <!-- Admin Login Activity -->
        <activity
            android:name=".AdminLoginActivity"
            android:exported="false" />

        <!-- Government Login Activity -->
        <activity
            android:name=".GovLoginActivity"
            android:exported="false" />

        <!-- Maps Activity -->
        <activity
            android:name=".MapsActivity"
            android:label="Maps"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.XavierProject" />

        <!-- Complaints Activity -->
        <activity
            android:name=".ComplaintsActivity"
            android:label="Complaints"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.XavierProject" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDUU5UwGe3VJNK-Yv1EpTXLc6xt_d1vbKA" />



    </application>

</manifest>
